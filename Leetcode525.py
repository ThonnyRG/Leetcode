class Solution:
    def findMaxLength(self, nums: List[int]) -> int:
        max_length = 0
        sum_map = {0: -1}
        count = 0
        
        for i, num in enumerate(nums):
            if num == 0:
                count -= 1
            else:
                count += 
                
        
        ret